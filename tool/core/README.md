# Tool Core

Shared tool adapter contract for Greentic digital workers.

This crate defines the common request, result, and descriptor models used by concrete tool backends such as:

- `component-adapter`
- `mcp-adapter`
