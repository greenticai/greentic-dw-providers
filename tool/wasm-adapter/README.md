# Tool WASM Adapter

WASM tool adapter anchor.

The shared contract uses:

- provider type `dw.tool.wasm-adapter`
- capability URI `cap://dw.tool.wasm`
- pack capability id `greentic.cap.tool.wasm`

This directory is currently documentation only.
